apiVersion: v1
kind: Service
metadata:
  name: fastapi-app
  namespace: production
spec:
  selector:
    app: fastapi-app
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP  # Pas besoin de NodePort avec Traefik
